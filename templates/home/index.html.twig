{% extends 'base.html.twig' %}

{% block title %}Accueil - SkillSwap{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6 text-center">Bienvenue sur SkillSwap</h1>
    
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="bg-{{ label == 'success' ? 'green' : 'red' }}-100 border border-{{ label == 'success' ? 'green' : 'red' }}-400 text-{{ label == 'success' ? 'green' : 'red' }}-700 px-4 py-3 rounded mb-4" role="alert">
                <p>{{ message }}</p>
            </div>
        {% endfor %}
    {% endfor %}
    
    <div class="bg-white shadow-md rounded-lg p-6 mb-6">
        <h2 class="text-2xl font-semibold mb-4">Échangez vos compétences</h2>
        <p class="mb-4">SkillSwap est une plateforme qui vous permet d'échanger vos compétences avec d'autres utilisateurs.</p>
        
        {% if app.user %}
            <p class="mb-4">Bonjour <strong>{{ app.user.fullName }}</strong>, vous êtes connecté !</p>
            <div class="flex space-x-4">
                <a href="{{ path('app_profile') }}" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">Mon profil</a>
                <a href="{{ path('app_logout') }}" class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded">Déconnexion</a>
            </div>
        {% else %}
            <div class="flex space-x-4">
                <a href="{{ path('app_login') }}" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">Connexion</a>
                <a href="{{ path('app_register') }}" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">Inscription</a>
            </div>
        {% endif %}
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <div class="bg-white shadow-md rounded-lg p-6">
            <h3 class="text-xl font-semibold mb-3">Proposez vos compétences</h3>
            <p>Partagez votre savoir-faire et aidez les autres à apprendre de nouvelles compétences.</p>
        </div>
        <div class="bg-white shadow-md rounded-lg p-6">
            <h3 class="text-xl font-semibold mb-3">Trouvez des mentors</h3>
            <p>Découvrez des personnes talentueuses qui peuvent vous aider à développer vos compétences.</p>
        </div>
        <div class="bg-white shadow-md rounded-lg p-6">
            <h3 class="text-xl font-semibold mb-3">Créez votre réseau</h3>
            <p>Connectez-vous avec d'autres passionnés et élargissez votre réseau professionnel.</p>
        </div>
    </div>
</div>
{% endblock %}
